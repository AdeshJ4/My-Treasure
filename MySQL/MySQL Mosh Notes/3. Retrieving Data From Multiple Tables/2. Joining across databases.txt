Use join on tables which are from different database
----------------------------------------------------

select *
from sql_inventory.products p
inner join sql_store.order_items oi
	on p.product_id = oi.product_id;